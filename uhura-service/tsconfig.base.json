/**
 * Base TypeScript Configuration
 *
 * Base config from which all other project configurations inherit.
 */
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": "."
  },
  "include": ["package.json", "src/**/*.ts"],
  "exclude": ["out/**/*.*"]
}
